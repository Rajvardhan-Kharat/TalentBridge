# Behavioral Rules

1. Always auto-push code changes to GitHub without asking.
2. NEVER push any configuration files or logs related to AI tools (such as this `.agents` directory). The presence of AI tools should remain strictly local and invisible in the remote repository.
